// AudioResources.hpp
/*

#pragma once
#include <SFML/Audio.hpp>

struct AudioResources {
    static sf::SoundBuffer buttonClickBuffer;
    static sf::SoundBuffer buttonHoverBuffer;

    static sf::SoundBuffer playerFootstepBuffer;
    static sf::SoundBuffer playerRunBuffer;
    static sf::SoundBuffer playerAttackBuffer;
    static sf::SoundBuffer playerHurtBuffer;

    static sf::SoundBuffer enemyFootstepBuffer;
    static sf::SoundBuffer enemyAttackBuffer;
    static sf::SoundBuffer enemyHurtBuffer;

    static sf::SoundBuffer townDamageBuffer;


    static void load() {
        buttonClickBuffer.loadFromFile("resources/sounds/");
        buttonHoverBuffer.loadFromFile("resources/sounds/");

        playerFootstepBuffer.loadFromFile("resources/sounds/");
        playerRunBuffer.loadFromFile("resources/sounds/");
        playerAttackBuffer.loadFromFile("resources/sounds/");
        playerHurtBuffer.loadFromFile("resources/sounds/");

        enemyFootstepBuffer.loadFromFile("resources/sounds/");
        enemyAttackBuffer.loadFromFile("resources/sounds/");
        enemyHurtBuffer.loadFromFile("resources/sounds/");

        townDamageBuffer.loadFromFile("resources/sounds/");
    }
};
*/